# trimctx v0.2 Stabilization Refactor Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore the Windows release quality gate, align integration documentation with actual side effects, and split stable CLI responsibilities without changing user-visible behavior.

**Architecture:** Keep Commander wiring in `src/cli.ts`, move command implementations into focused modules, and introduce small shared modules for output safety, analysis option parsing, the analysis pipeline, and session discovery. Preserve `src/core/session.ts` as a compatibility facade while internal consumers move to the new boundaries.

**Tech Stack:** Node.js 20+, TypeScript, commander, vitest, npm package smoke tests.

---

### Task 1: Fix Windows packed-install path detection

**Files:** `tests/package-contents.test.ts`

- [x] Confirm the existing smoke fails because Windows installs global modules under `<prefix>/node_modules`.
- [x] Add a platform-aware `installedPackageRoot(prefix)` helper and replace the hard-coded Unix path.
- [x] Run the focused packed-install smoke and expect PASS.

### Task 2: Extract shared CLI primitives

**Files:** `src/platform/files.ts`, `src/commands/shared/analysis-options.ts`, direct unit tests, `src/core/compressor.ts`, `src/cli.ts`

- [x] Write failing tests for same-path rejection and analysis option validation.
- [x] Implement file safety and CLI option helpers without changing error text.
- [x] Migrate compressor and CLI consumers.
- [x] Run the new unit tests plus compressor and CLI tests.

### Task 3: Separate pipeline and session discovery

**Files:** `src/core/pipeline.ts`, `src/sessions/discovery.ts`, `src/core/session.ts`, `src/core/hook.ts`

- [x] Move `analyzeFile` into the pipeline module unchanged.
- [x] Move session discovery and no-session help into the sessions module unchanged.
- [x] Keep `src/core/session.ts` as a re-export facade.
- [x] Run session-related CLI and hook tests.

### Task 4: Split CLI command registration

**Files:** `src/commands/analysis.ts`, `src/commands/new-chat.ts`, `src/commands/integrations.ts`, `src/cli.ts`

- [x] Move root/current/analyze/report/compress registration into `analysis.ts`.
- [x] Move new-chat/handoff registration and package writers into `new-chat.ts`.
- [x] Move init/hook/install-hooks registration and helpers into `integrations.ts`.
- [x] Keep package root/version dependency injection explicit.
- [x] Run CLI, hook and package tests.

### Task 5: Align integration and command documentation

**Files:** `README.md`, `README_zh.md`, `docs/user/usage*.md`, `docs/dev/roadmap.md`, `docs/dev/iteration-plan.md`, `docs/dev/status-and-next-steps.md`

- [x] Document SessionStart environment binding and Stop hook `.claude/CLAUDE.md` writes.
- [x] Remove claims that installed hooks never write project files.
- [x] Keep `current --compress` only as advanced/legacy behavior.
- [x] Describe packaged integrations as implemented but experimental while further expansion remains frozen.

### Task 6: Final verification

- [x] Run `npm test`.
- [x] Run `npm run build`.
- [x] Run `git diff --check`.
- [x] Run `npm pack --dry-run --json` and confirm client assets remain included.
- [x] Preserve the user's existing `.claude/settings.local.json` change.
